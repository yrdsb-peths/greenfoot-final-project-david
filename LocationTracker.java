import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class LocationTracker here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class LocationTracker extends ScrollActor
{
    /**
     * Act - do whatever the LocationTracker wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    
    public LocationTracker(){
        setImage(new GreenfootImage(10,10));
    }
}
